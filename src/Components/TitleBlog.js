import React from "react";

export default function TitleBlog() {
  const styles = {
    title: {
      margin: "0px",
      color: "#FFBB00",
      fontSize: "12px",
      textAlign: "left",
    },
  };

  return (
    <div>
      <p style={styles.title}>B L O G</p>
    </div>
  );
}
