import React from "react";

export default function TitleDamogo() {
  const styles = {
    title: {
      margin: "0px",
      fontSize: "12px",
    },
  };

  return (
    <div>
      <p style={styles.title}>D A M O G O</p>
    </div>
  );
}
